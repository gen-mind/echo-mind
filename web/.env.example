i # EchoMind Web UI Configuration
# Copy to .env and configure for your environment
#
# REQUIRED variables are marked with [REQUIRED]
# Optional variables have sensible defaults

# ============================================
# API Configuration
# ============================================

# API Base URL - where the backend API is hosted
# In development: proxied through Vite (default: /api/v1)
# In production: set to your API server URL (e.g., https://api.example.com/api/v1)
VITE_API_BASE_URL=/api/v1

# WebSocket URL for chat streaming
# In development: proxied through Vite
# In production: set to your WebSocket server URL
VITE_WS_URL=

# ============================================
# OIDC Configuration (Authentik)
# ============================================

# [REQUIRED] OIDC Authority URL - your Authentik OpenID Provider endpoint
# Example: https://auth.example.com/application/o/echomind/
VITE_OIDC_AUTHORITY=

# [REQUIRED] OIDC Client ID - from your Authentik OAuth2 Provider
VITE_OIDC_CLIENT_ID=

# [REQUIRED] Redirect URI after login - must match Authentik config
# Example: https://app.example.com/auth/callback
VITE_OIDC_REDIRECT_URI=

# Redirect URI after logout (defaults to origin)
VITE_OIDC_POST_LOGOUT_REDIRECT_URI=

# OIDC Scopes (defaults to: openid profile email offline_access)
# offline_access is required for refresh tokens
VITE_OIDC_SCOPE=openid profile email offline_access
